const nilai = 90;
let ucapan;

if (nilai >= 80){
    ucapan = "anda lulus";
}else{
    ucapan = "anda tidak lulus";
}

console.log(`${ucapan}`);

// ternary operator

const total = 0;
const hasil = total >= 75 ? "lulus" : "tidak lulus";
console.log(`${hasil}`);